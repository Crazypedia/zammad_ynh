#!/bin/bash
set -euo pipefail
source /usr/share/yunohost/helpers

# This draft keeps upgrades simple: refresh nginx template & ensure services
ynh_add_nginx_config
systemctl daemon-reload || true
systemctl restart zammad zammad-web zammad-worker zammad-websocket || true
